# 真机 10 件修复批次复验

日期：2026-09-23。原稿为 `order/北京知识产权10件.rar`；本轮输出为 `order/北京知识产权10件_已处理_20260923_114920`。旧的 `..._20260922_213656` 保留作问题对照，不作为本轮交付。

## 修复与证据

- 年份规则由用户补充确认：只把早于 2025 的有效图片日期年份更新为 2025；2025 和未来日期保持原样。日期桥接只接受有效年月日或明确的年月字段，直接在原图 OCR 框中定位年份；不会把编号、刻度、时间片段当作年份。白字蓝底截图改用原字形高对比颜色和逐行背景恢复，避免灰字与涂抹。
- 真实输入共 24 张图片更新：医院人力资源说明 11 张（14 个 `2022→2025` 字段），弘元蓝宝石切片机说明 13 张（13 个 `2024→2025` 字段）。报告 HTML 有 24 张原图/处理后完整图和局部放大图，按日期桥接的 `media_path` 精确选图，不混入 Word 排版导致的其他图片变化。人工抽看了两类界面图与汇总局部图；报告仍将自动比对标注为“待核验”。
- 医院 `image30.png` 中的未来 `2033`、建筑 `image11.png` 图表刻度及 `image18.png` 的 `2026`、毫米波 `image3.png` 中的 `2026` 均与原稿字节一致；毫米波说明全部图片未被日期桥接修改。
- 首次 Word 输出对 7 份说明报“仍存在后续重复封面标题块”。真实 PDF 第 2 页附近确有残留旧封面。清理规则修复后，以现有 DOCX 为输入仅重跑这 7 份的重复封面删除和 PDF 导出，7/7 Word QA=OK；其余 3 份初次即 OK。医院和蓝宝石说明的 24 张修正图在 Word 保存后与核验副本逐字节一致。
- 用户指出的 5 份标题正文冲突材料均在报告中提示人工复核，其余 5 份未误标。视觉模型仅抽查图片疑难样本：本轮 20 次调用，13 次给出可用判断，3 次接口失败，合计 28,389 tokens；不参与 OCR 拦截和自动日期写入。
- GUI/CLI 可选择 `manual,code,txt_placeholder`，默认全选。预览和执行使用同一选择；未选阶段报告 `SKIPPED`，不调用其扫描/处理入口，进度只计所选阶段。TXT 单选被拒绝，因为仍是占位流程。

## 最终验收

报告：`.omc/full-batch-corrected-20260923/combined_document_result.html` 及同名 JSON，状态 `DOCUMENT_PROCESSING_OK`，失败 0。7 份封面复检明细在 `cover_retry_seven_result.json`；初次失败证据保存在 `combined_cover_result_initial.json`。

输出中 20 个 DOCX 的 ZIP 结构正常，20 个 PDF 均可读取（合计 857 页）；代码阶段 10/10 成功并清理 1100 处中文注释；10 个 TXT 与原稿 SHA-256 一致；未留临时文件。全套单测 132 项通过。最终 RAR 未打包。
