# 真机输出回归：封面残片与标题正文相关性

日期：2026-09-23

输入：`D:\360MoveData\Users\ban\Desktop\work_about\order\北京知识产权10件_已处理_20260922_213656`。原目录只读；最终脚本的 Word 复测使用项目内 `.omc/verification/cover_20260923_final` 的三份副本。

## 封面残片

原 PDF 第 2 页仍可见：蓝宝石切片机的重复软件名、医院人力资源的“用户手册”、建筑项目的“技术说明手册”和扉页元数据。原因是旧清理逻辑遇到残片与正文或目录同页时保留整页。

修复后在交互式 Word COM 中运行三份副本，`cover_result.json` 三项均为 `OK`、无失败；PDF 第 2 页分别直接进入正文、概述、目录。建筑项目说明由 13 页变为 12 页，目录仍在。三份输出 DOCX ZIP 完整性检查均通过，媒体文件数分别为 44、18、13，且与原真机 DOCX 的媒体内容哈希逐项一致。封面清理增加了对分页/分节边界、表格、行内图片和浮动图片锚点的保护；原真机输出未覆盖。

## 标题与正文

先前统一流程只对约四分之一图片做 OCR 相关性抽样，正文全文未参与标题相关性判断。现在新增保守的正文主题预检，仅在报告中提示人工复核，不改变图片拦截、批量熔断或年份写入。

用户确认的五份错配全部触发 `review_topic_mismatch`：毫米波组件参数校验、无源射频芯片数据调控、弘元绿能金刚线切片机控制、太阳能 LED 路灯储能与充放电、智能 LED 路灯自适应调光。同批其余五份均为 `unknown`，未误报。该结果表示发现值得复核的主题证据，不等同于模型完成了语义正确性证明。

OCR 无字的截图仍为 `unknown`。视觉内容相关性需要视觉模型或人工查看；正文文本相关性不依赖多模态 API。

## 回归命令

项目根目录运行 `python\python.exe -m unittest discover -s tests -q`：94 项通过。Python 编译、PowerShell 解析、`git diff --check` 均通过。封面 Word 复测报告和 PDF 保留在 `.omc/verification/cover_20260923_final`。最终 RAR 未打包。
