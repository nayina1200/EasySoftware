# 用户确认的阶段决定

状态：Confirmed  
证据：2026-09-22 当前任务对话。

- 代码任务部分已经完成，无需继续修改和验证。
- 当前动作仅为整理 Handoff，供其他对话接续下一期。
- 说明类文件在代码阶段完全不动。
- 清理后的代码 DOCX 生成 PDF 并覆盖输出副本中的旧 PDF。
- 处理完成后暂不压缩 RAR；压缩是所有期次结束后的最后步骤。
- 当前默认保留执行前预览；稳定后才允许跳过。
- 所有后续期次都必须显示真实进度及已处理件数，并复用项目现有进度实现。
- 历史样例输入：`D:\360MoveData\Users\ban\Desktop\work_about\order\武朕9.16.rar`。

## 说明阶段确认（2026-09-22）

- 一次点击包含说明、代码、TXT 占位；TXT 三期暂不读写。
- 不可并行时顺序为说明 → 代码 → TXT；Word COM 必须串行。
- OCR 无有效文字不停止；只有高置信不相关才可能拦截。
- 单项目不相关占决定性样本超过 50% 才拦截；被拦截项目超过批量 20% 时熔断日期写入。
- 截图日期只把年份改为 2025，保留月日和格式。
- 正文年份只有语义判断为需要时效更新且置信度至少 0.85 时改为 2025。
- 说明书不强制 A4，沿用原页面尺寸；残缺副标题不补全。
- 最终 RAR 仍不打包。

## 真机复核追加决定（2026-09-23）

- 执行前可勾选说明书、代码、TXT 占位阶段；未选阶段跳过，预检和执行使用同一选择。TXT 仍只占位，单选 TXT 不执行。
- 图片中的日期仅把早于 2025 的年份改为 2025；2025 和未来年份保持原样。用户特别指出图四的未来日期不可修改。
- 报告须展示确实变更的年份图片原稿与处理后对比；旧批次含误改，不作为交付结果。
