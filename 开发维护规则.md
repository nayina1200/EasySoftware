# EasySoftware 开发维护规则

## 1. 文档地位

本文件是 EasySoftware 后续设计、修改、排错和测试的必读规则。

- 修改功能前，先阅读本文件、`README.md`、`SKILL.md` 和相关测试。
- 本文件记录的是用户已确认的实际工作规则；若与旧脚本注释、旧报告样式或 `SKILL.md` 的描述冲突，以本文件为准，并同步更新冲突文档。
- 添加、修改或优化任何功能时，必须先保护并验证原有功能正常运行；任何流程、检查项、修复项或审核信息的删除，都必须有明确的用户确认。
- 修改后必须优先运行相关测试；涉及流程、报告、打包或公共函数时，运行完整测试集。
- 全局优化原则：在保证材料完整、修复准确、终检闭环和人工审核清晰的优秀质量基础上，尽可能提高处理速度；不得为了提速跳过必需检查、降低修复质量、使用未经验证的缓存或减少审核证据。
- DOCX 是所有 Word/PDF 材料的唯一编辑源。修复 PDF 前必须先修复 DOCX，再由 Word 导出 PDF；Word 导出超时时，允许使用 LibreOffice 从当前 DOCX 导出 PDF并继续终检。禁止直接在 PDF 上叠加、覆盖或写入任何页眉、页码或正文内容。

## 2. 程序目标和边界

EasySoftware 用于批量审核、修复和交付中国软件著作权材料。每个项目通常包含：

1. TXT：软件名称、主要功能等文本信息。
2. 说明书 DOCX/PDF：封面、正文、界面截图、页眉、页码。
3. 代码 DOCX/PDF：完整代码材料、页眉、版本、页码。

程序的目标是让人工审核者快速判断“项目是否合格、哪里被修过、修复后是否合格”，而不是展示程序内部执行细节。审核页和报告必须使用普通中文说明，避免只输出技术术语或原始日志。

## 3. 文件安全和目录规则

- 输入压缩包和 `解压版` 永远保持原样，不得修改、覆盖或删除。
- 只有 `修改版` 可以被程序修复。
- 处理单个压缩包前，先新建与压缩包同名的目录，并将压缩包移动到该目录后再开始处理。
- 单个压缩包默认生成同级 `压缩包名_解压版`、`压缩包名_修改版`；批次目录使用 `解压版`、`修改版`。
- 支持 ZIP、RAR、7z 的递归解压；顶层交付只支持 ZIP 或 RAR，并优先保持输入压缩包格式。
- 处理续跑必须复用同一个 `work_dir` 和同一份源材料状态。发现双重嵌套目录时，要自动定位实际材料目录，不能把压缩包路径再次拼接到自身。
- 用户手工修正后的 `修改版` 视为有效成果。复现问题必须复制 `解压版` 到临时目录执行，严禁覆盖用户手工修正的文件。
- 用户反馈某个具体目录的问题时，该目录视为只读反馈样本；分析、复现、修复验证和 PDF 导出均必须在自行创建的临时副本中完成，主目录不得写入。
- 缓存由源文件 SHA-256 和规则版本共同决定。修复规则改变时必须提升版本或使对应缓存失效。
- 临时目录可能与材料目录不在同一磁盘；跨盘交付文件必须先复制到目标目录的临时文件，再在目标磁盘内原子替换，禁止直接用跨盘 `os.replace` 或 `Move-Item`。

## 4. 对外入口和脚本职责

- 图形界面入口：`启动EasySoftware.ps1`、`启动EasySoftware.cmd`、`EasySoftware.lnk`。
- 命令行唯一公共入口：`scripts/unified_workflow.py`。
- 批处理主逻辑：`scripts/process_batch.py`。
- 说明书 Word 修复：`scripts/word_manual_pipeline_stable.ps1`。
- 代码 Word 页眉修复：`scripts/repair_code_headers.ps1`。
- 代码 DOCX 页眉页码修复并导出 PDF：`scripts/repair_code_headers.ps1`。
- 最终打包：`scripts/package_approved_modified.py`。

正常流程不得让用户手工拼接低层脚本。低层脚本只用于定位已知故障、编写测试或修复入口返回的明确错误。

## 5. 固定处理流程

### 5.1 准备与初检

1. 准备压缩包目录，解压并识别真实项目目录。
2. 为每个项目识别 TXT、说明书 DOCX/PDF、代码 DOCX/PDF，记录原始路径、哈希和缺失文件。
3. 审计 TXT、说明书结构、代码首尾页和界面图片。
4. 生成第一轮图片审核页。

### 5.2 第一轮图片审核

- 第一轮的主要目的，是尽早发现需要退回的界面图片问题，避免对已确定退回的项目浪费 Word、PDF 修复时间。
- 页面按项目序号和名称展示，并提供该项目说明 PDF 的直接打开链接。
- 用户只勾选“图片不通过、需要退回”的项目；未勾选的项目继续自动处理。
- 首轮页面应显示图片不通过的具体原因，例如配色明显不一致、图片重复、登录界面异常或缺失。
- 不要把所有项目默认标成“图片不通过”，勾选控件的语义必须明确为“勾选退回”。

### 5.3 自动修复与终检

1. 仅对首轮未退回项目复制到 `修改版` 并处理。
2. 清理 TXT 的主要功能字段。
3. 修复说明书 DOCX，并导出对应 PDF。
4. 修复代码 DOCX 和代码 PDF。
5. 对修复后的 DOCX/PDF 重新检查。
6. 生成第二轮最终审核页。

### 5.4 第二轮最终审核和打包

- 第二轮用于审核修复成果，不重复展示第一轮已经人工审核过的说明书界面图片发现。
- 每个项目的结果必须直接说明：原来发现什么、程序修了什么、修好没有、仍需要人工处理什么。
- 用户从最终审核页选择通过项目，程序只打包通过项目。
- 打包完成后报告交付文件路径、项目数、大小、哈希和完整性检查结果。

## 6. TXT 检查和修复规则

### 6.1 检查范围

TXT 的自动文字清理只作用于 `主要功能：` 或 `软件主要功能：` 字段的内容，不修改其他字段或其他行。原始编码必须保持，例如 UTF-8 BOM、UTF-16、GB18030 均按原编码写回。

### 6.2 必查问题

- 主要功能为空、缺中文、缺可识别功能动作。
- 括号：半角和全角圆括号。
- 全部中文和英文双引号。
- 序号与序数词，例如 `1、`、`（一）`、`第一、`、`首先、`。
- 连续或混合异常标点，例如 `。。`、`，，`、`，。`、`！。`、`！ 。 ，`、`::`。
- 标点位于开头、末尾或造成明显不通顺的情况。

### 6.3 修复规则

- 圆括号替换为中文逗号。
- 删除全部中文和英文双引号。
- 删除序号和序数词，普通业务数字、年份、版本号等非序号数字必须保留。
- 只要连续出现两个或以上异常标点，删除后续符号，仅保留第一个符号；中文/英文标点混用和中间仅有空格的情况同样处理。
- 修复后不得改变 TXT 编码和非主要功能字段。

### 6.4 TXT 报告和页面表现

- 最终审核页必须提供“主要功能模块”修改前后对比。
- 发生改动的字词或符号必须高亮，便于人工快速复核。
- 报告使用清晰中文说明，例如“删除 2 个双引号”“将 1 处括号替换为逗号”“删除 3 个重复标点”。
- 原始 TXT 存在时不得显示“未提供 TXT”。

## 7. 说明书 DOCX/PDF 规则

### 7.1 封面

- 已存在有效封面（软件名称加“说明书”或等效说明文字）时，不重复插入封面。
- 封面必须独占第一页；封面与正文位于同一页时，必须在封面后插入分页或分节。
- 如果原封面缺少软件名称、缺少说明书类标签、标题未居中，或名称与说明书标签的字号相差过大，删除原封面文字块并重新生成标准封面；整页没有明确封面碎片时只新增封面，不删除正文。
- 第一页仅有软件名称或仅有“操作手册/使用说明书”等封面碎片时，删除整页并一并清除遗留分页符、分节符后再生成新封面；若该页还有正文、目录、表格或图片，只删除封面碎片及相邻空段，不得删除正文。
- 封面前两页的审核拼图是最终审核的重要证据，必须保留并按一个项目两页一组展示。
- 标题使用 Word 自动编号时，新增封面或调整分页不得复制、重置或产生重复编号。
- 查找“软件名称 + 说明书/手册”封面块时，标题后最多查看 6 段，但只允许空段存在于两者之间；一旦遇到任何非空正文、章节标题、目录或其他内容，必须立即停止查找，不得继续命中后续标签，更不得删除这些内容。
- 已识别的名称和说明书标签若与任何正文或章节标题共用首页，必须保留两行及全部同页内容：整理两行封面格式并在说明书标签后插入分页。只有整页确实仅包含封面碎片时，才允许删除后重建封面。
- 未识别到有效“说明书/手册”标签而首页已经出现软件名称、章节或正文时，只能在文档开头新增标准封面并将原内容整体后移；不得把原软件名称、概述、章节、正文、表格或图片当作重复封面删除。后续页面的所谓重复封面只要同页含有任何正文、章节、表格或图片，也必须保留并在审核页提示人工确认。
- 修复后封面标题或说明书行仍带自动编号时，保留当前修复结果，不回滚；最终审核页明确标红提示“封面自动编号仍需人工确认”。
- 不得因封面 QA 失败而回滚已完成的页眉、页码、双引号、年份等其他修复。

### 7.2 正文与文字

- 删除说明书全文中的中文和英文双引号。
- 文本中出现年份语境时，将年份表达替换为“目前”，例如“系统利用 2025 年提出的”处理为“系统利用目前提出的”。
- 年份和双引号修复必须输出诊断、修改前后对比和高亮差异；对比必须从同一原段落直接计算，严禁按 Word 段落序号在修复前后硬配对，避免封面分页、列表或字段刷新后显示串段。
- 说明书的普通正文以全文中最常见的字体、字号、加粗和斜体格式为准；发现不一致时统一修复全部异常普通正文段落，并在结果中报告修复段数。
- 普通正文必须同时满足：Word 正文级别、不是封面名称或说明书标签、不在表格内、未使用 Word 自动编号，且行首不是数字序号、汉字序号、章节号或项目符号。标题、编号列表、封面和表格内容不得按正文格式修复。
- 图片说明的字体、字号、加粗和斜体应与普通正文一致。Word 题注样式，以及“图 1”“表 1”“截图 1”等短图片说明，也应参与正文格式统一；图片本身、说明文字的位置、对齐和编号不得修改。
- DOCX 前置检查读不到直接字体名、但正文仍有直接字号或字形设置时，必须安排 Word 读取有效样式并统一，不能把该段误判为“无样式信息”而跳过。
- 对“标题内混入图片或正文”的特殊文档，仅在可以稳定定位到第一个标题下第一个小标题正文末尾时自动迁移；定位不稳定时保留原文并在报告提示，避免破坏结构。

### 7.3 页眉、版本和页码

- **全局联动规则**：任何页眉、页脚或页码的修复都必须一次应用到整份 DOCX，而不是只修改当前检测到的一页。处理范围覆盖所有分节、首页、普通页、奇数页和偶数页；一处缺失被补齐后，其余缺失页面同步补齐。
- **原样优先规则**：只要原材料任一生效页面已有页眉、页脚或页码，必须将它视为模板，保留原有文字、位置、字体、对齐、`PAGE` 字段周边格式和装饰符号。例如 `第 {PAGE} 页`、`-- {PAGE} --`、单独的 `PAGE` 都保持原样；不得删除后改成程序自己的 `名称 V1.0 + PAGE` 样式。
- 每个实际页面必须有页眉，且只能有一个动态 `PAGE` 页码；页码可以位于原有页眉或原有页脚。已有页面的页码位置与格式不变，缺失页面从原有模板补齐。只有整份材料都没有页眉或页码时，才新增标准页眉 `TXT 软件名称 + 空格 + V1.0`（9 号宋体）和动态 `PAGE`。
- 未触发页眉、页脚或页码修复时，不改动原有内容；触发修复时也只补缺失项，不得删除原有页眉、页脚、页码或页码周边文字。页脚中与页码无关的原有文字必须保留。
- 封面不可因“首页不同”设置而丢失规定页眉、版本或页码。
- Word 与 PDF 必须内容一致：说明书和代码都只能先修改 DOCX，再由 Word 或超时备用转换器从 DOCX 导出 PDF；名称、`V1.0`、页眉、页码、页数和封面正文分页结果都要同步验证。禁止直接修改 PDF。
- DOCX 保存或 PDF 导出失败时，保留已经成功的文件，报告失败原因；严禁把整份文件回滚为原版而掩盖已完成修复。

## 8. 代码 DOCX/PDF 规则

### 8.1 独立中文注释清理阶段

- 公共入口仍为 `scripts/unified_workflow.py`；`--clean-code-comments` 只预览，`--apply-comment-cleanup` 确认执行。
- 该模式只处理同时存在同名 PDF 的 `*代码.docx`；TXT 和说明 DOCX/PDF 原样复制，不得读写其内容。
- 删除 Java/JavaScript/CSS、Python、HTML 和 SQL 中至少含一个汉字的已确定注释；纯英文注释保留。字符串、模板字符串、JavaScript 正则、HTML 正文和 Python 三引号内容不得删除。
- 未闭合或其他无法确定的内容保留，在 HTML/JSON 报告中标记人工复核。
- 原 RAR 永不移动、不修改。执行结果输出到时间戳新目录；本阶段不打包。
- 代码 PDF 只能由清理后 DOCX 导出。新 PDF 通过非空、可打开且页数大于零的验证后才原子替换输出副本中的旧 PDF。

- 代码材料按文件顺序输出完整 PDF；无需另行输出“原版完好部分 + 修复部分”的两份 PDF。
- 代码 DOCX 是代码 PDF 的唯一来源。任何修复先写入 DOCX，再由 Word 导出完整 PDF；禁止直接修改代码 PDF。
- 每页只能有且必须有一个页眉、一个页码。检查既有页码时不受其原始位置影响：已有且正确时不添加；缺少、重复或不正确时只整理为一个规范页眉和一个规范页码。
- 规范页眉为 `TXT 软件名称 + 空格 + V1.0`，使用 9 号宋体（SimSun）；页码为连续动态 PAGE 字段，位于页眉右侧。
- 如代码材料缺页码，通常视为全文均缺。必须补齐每一页，不只补首尾页。
- 如代码材料缺页眉，必须补齐每一页，不只补检测到缺失的页面。
- Word 的页码必须是动态 PAGE 域，并设置打开时刷新字段；禁止只写死第一页的数字。
- Word 关闭“首页不同”或“奇偶页不同”后，DOCX 压缩包仍可能保留未使用的空白页眉 XML；检查时只读取各分节当前实际生效的默认、首页或偶数页页眉，不能因此误报缺页眉或触发重复修复。
- PDF 必须由修复后的 DOCX 按同一名称、版本和连续页码重新导出。代码 Word 单项目导出保护时间为 20 秒；超时后先关闭本次 Word，再使用 LibreOffice 从 DOCX 导出并继续最终 QA；不得出现 DOCX 已修好、PDF 未导出或 PDF 仍缺页眉页码的情况。
- 缺少配套代码 DOCX 时，报告“需要代码 DOCX 重新导出”，保留原 PDF 并标红等待人工处理，绝不直接修改 PDF。
- 最终检查至少验证代码 PDF 首尾页的软件名称、`V1.0` 和正确页码；同时验证代码 DOCX 页眉、版本和 PAGE 字段。

## 9. 图片和登录界面规则

- 说明书界面图按文档顺序抽取，保留原始分辨率并生成审核图。
- 检查明显重复图、界面主配色明显偏离、无效占位图和登录页问题。
- 配色检查不能只给“未发现明显不一致”的笼统结论；发现偏离时指出对应图片序号和原因。
- 登录界面以截图 OCR 为主要证据，按顺序寻找；OCR 首轮采用稀疏界面文字识别，证据不足时再用灰度增强和放大识别。发现第一个可靠登录界面后，必须继续检查其后的 6 张截图；期间识别到第二个可靠登录界面立即报警，检查完成后停止。
- 登录判断至少要求两类证据组合，例如“登录 + 用户名”“用户名 + 密码”“登录 + 验证码”。只有单个“登录”字样不算可靠登录页。
- 紧邻截图的 Word 说明文字只能作为 OCR 的辅助确认，截图自身至少要识别到一项登录相关文字；OCR 完全为空时，不得只凭正文或图注把图片判为登录页。正文中单独提到“登录”或“登录 + 密码”不得判为登录界面。账号/帐号/用户名/用户账号/登录名/账户、密码/口令、登录/进入系统、验证码/记住密码/忘记密码均属于可识别证据。
- OCR 采用 Tesseract 优先、Windows 自带中文 OCR 后备。两个组件都不可用时，首轮页面必须显示“OCR 组件不可用，登录界面尚未完成检查”，不得误报为“没有登录页”。
- 一个项目识别出两个或以上可靠登录界面时，在第一轮审核页报警。
- 检测到两个或以上可靠登录界面时，首轮界面拼图必须将对应图片分别标注为“登录页 1/2”等顺序，并使用醒目的红色标题、文字和粗边框；不能只在文字报告中报警。
- 一个应有登录功能的管理类项目未识别到登录界面时，也在第一轮审核页报警。
- 第一轮勾选退回的项目不应进入后续自动修复、最终审核或交付包。

## 10. 审核页和报告规则

### 10.1 总体要求

- 页面首先服务人工审核速度：项目名称和序号醒目，状态一眼可见。
- 合格模块显示绿色，不通过或仍需人工处理的模块显示红色。
- 不用专业晦涩的内部术语替代结论；必要技术信息放在“详细信息”而非主结论。
- 同一项目的名称和序号只显示一次，不能让说明页和代码页分别孤立标注。

### 10.2 第一轮页面

- 显示项目序号、名称、说明 PDF 直链、界面审核图片、登录检查结论和退回勾选框。
- 显示图片问题与登录报警，不显示最终修复结果。
- 底部提供继续处理未勾选项目的操作。

### 10.3 第二轮页面

- 顶部先显示项目总成果列表：项目序号、名称、TXT 原始情况与修复、代码原始情况与修复结果、说明书原始情况与修复结果。
- 列表描述项目成果，例如“代码：已补齐页眉和 100 页页码，最终通过”，而不是“脚本执行成功”。
- TXT 显示主要功能修改前后高亮对比。
- 说明书不重复显示第一轮界面图片问题或界面图片结果。
- 说明书封面前两页拼图保留，两个页面并排为一组，方便确认封面、分页、页眉和页码。
- 代码保留首尾页拼图，便于确认名称、版本、页眉和页码。
- 仍有问题时明确给出文件、位置、原因和是否需要人工处理。

## 11. 进度条和性能规则

- 进度条必须是实际进度，不可长期只显示循环动画。
- 显示当前模块、当前 RAR、当前项目名称、已处理项目数、待处理项目数、失败数、模块进度、已用时间和预计剩余时间。
- 分母必须使用当前模块实际计划处理数量，不能错误使用全批次项目数。
- 预览、扫描、正式执行和 PDF 导出都必须接入同一 `progress.json` 真实进度契约；必须先生成全批次任务清单再处理，`current` 只能单调递增，结束时必须等于 `total`，`failures` 必须累积且不回退。
- 本规则适用于现有流程和后续所有新增工程/期次；新功能不得自建另一套跑马灯式进度。
- Word 处理应复用单个 Word 进程，并对每个可处理文档一次完成打开、修复、保存和导出。
- 可以同时打开多个程序处理不同资料；扫描、图片审核和报告生成可并行。涉及 DOCX 修改或 PDF 导出的 Word 自动化必须全局排队，同一时刻只允许一个 EasySoftware 任务控制 Word，防止并发 Word COM 导致文件锁定、RPC 断开或报告丢失。排队状态必须在进度栏中明确显示。
- 图片审核只渲染必要页面：说明书封面前两页、代码首尾页；不要渲染无用中间页。
- 缓存复用必须正确：源文件或规则版本变化后不能使用旧审核图、旧报告或旧修复结果。
- 单项目长时间卡住时，进度中必须可见正在执行的具体模块；超时后记录明确原因，并继续处理其他项目或保留可恢复状态。

## 12. 失败处理规则

- 每个项目独立失败，不能因一个 DOCX/PDF 异常中断整个批次。
- 已完成的修复必须保留，除非写入过程本身损坏文件。
- QA 不通过表示“保留修复结果并在审核页标红”，不是“自动恢复原文件”。
- 报告必须区分：原始问题、实际执行的修复、最终仍存在的问题。
- “未提供文件”和“文件存在但未被识别/未处理”必须区分，优先修复识别错误。

## 13. 修改和验收清单

每次修改前：

1. 阅读本文件，并定位受影响的流程、报告、缓存和测试。
2. 判断修改是否会影响原版/修改版隔离、两轮审核、Word/PDF 一致性或打包。
3. 对用户已手工修正项目，只使用临时复制的原始材料复现。

每次修改后：

1. 为新规则或已修复缺陷新增、调整回归测试。
2. 运行受影响测试；涉及主流程时运行 `python -m unittest discover -s tests -v`。
3. 至少使用一份临时副本验证实际 DOCX/PDF：检查封面、分页、页眉、页码和最终审核图。
4. 确认 TXT 保持原编码，原始文件未被修改，交付仅包含审核通过项目。
5. 更新本文件、`README.md` 或 `SKILL.md` 中因本次规则变化而过时的说明。
6. 新修复或规则修改必须完成验证闭环后才能上线：相关自动测试通过、至少一份临时复制材料完成真实 DOCX 修复和 PDF 导出、最终检查与审核页均确认预期结果；任一环节未完成即标记为未上线，不能作为批量处理依据。

## 14. 当前已知重点回归案例

## 15. 同句字体一致性规则

- 以 `。！？；!?;` 作为句子边界；同一句中的中文文字不得混用字体、字号、加粗或斜体。
- 普通正文发现同句字体混用时，纳入正文格式统一修复，并在最终检查中再次确认。
- 标题发现同句字体混用时，必须在审核结果中标红列出段落和原句；标题不套用正文格式。
- 中文与英文、数字分开判断，避免英文缩写、产品名称、公式或版本号造成误报。

后续涉及说明书、代码页眉页码或封面时，优先在原始解压版临时副本复现这些类型：

- 代码 DOCX 已有页眉但 PDF 未同步生成页眉。
- 代码 DOCX/PDF 均缺整份页码。
- 代码材料仅缺页眉。
- 说明书因封面 QA、双引号或自动编号问题而误回滚已修复文件。
- 封面与正文同页、标题采用 Word 自动编号、已有有效封面、封面正文混入同页。
- 主要功能中含括号、双引号、序号、序数词、混合连续异常标点。
- 管理类项目没有登录页，或出现两个登录页。

这些案例的修复验收标准始终是：原件不动、修改版内容正确、Word 和 PDF 一致、审核页能让人工一眼看懂结果。
